# Learnify Backend

Backend API для образовательного приложения Learnify.

## Статус

🚧 **В разработке** — backend часть будет реализована позже.

## Планируемый стек

- Node.js / Python
- REST API / GraphQL
- База данных (PostgreSQL / MongoDB)
- JWT аутентификация

## API эндпоинты (планируемые)

- `GET /api/courses` — список курсов
- `GET /api/courses/:id` — детали курса
- `GET /api/lessons/:id` — урок с карточками
- `POST /api/progress` — сохранение прогресса
- `GET /api/training` — карточки для тренировки
- `POST /api/answers` — отправка ответов




